# /api/components
This folder has a lot of functions used within the `/api` folder.

For examples: `datacleaning.ts` is used for cleaning the data, `gmailauth.ts` is used for authorize the gmail api, and so on.